import { CameraPosition } from "../model/interfaces";

/**
 * Coordinaat naar het torentje in Den Haag. Kan worden gebruikt om de camera naar te laten zoeken bij het opstarten.
 */
export const cameraOptionsTorentjeDenHaag: CameraPosition = {
  cameraPosition: { lat: 52.0746075, lon: 4.31333437 }
};
