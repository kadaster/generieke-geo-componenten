import { ExtractImportsPipe } from "./extract-imports.pipe";

describe("ExtractImportsPipe", () => {
  it("create an instance", () => {
    const pipe = new ExtractImportsPipe();
    expect(pipe).toBeTruthy();
  });
});
